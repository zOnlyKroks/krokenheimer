# Requirements for local training on AMD GPU (ROCm)
# Install PyTorch with ROCm support first:
# pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.2

transformers>=4.36.0
trl>=0.7.0
peft>=0.7.0
datasets>=2.14.0
accelerate>=0.24.0
sentencepiece>=0.1.99
protobuf>=3.20.0